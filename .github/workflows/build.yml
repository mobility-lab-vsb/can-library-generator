name: Build and Release Python App

on:
  push:
    tags:
      - 'v*'

jobs:
  set_version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.extract.outputs.version }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: extract
        run: |
          VERSION=$(git describe --tags --abbrev=0)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

  build-windows:
    runs-on: windows-latest
    needs: set_version
    env:
      APP_VERSION: ${{ needs.set_version.outputs.version }}
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Inject version into main.py
        shell: pwsh
        run: |
          (Get-Content main.py) -replace '__version__ = .*', '__version__ = "${{ env.APP_VERSION }}"' | Set-Content main.py

      - name: Build Windows executable
        run: |
          pyinstaller --clean --onefile --windowed --add-data "png;png" --name "CAN_Library_Generator" main.py

      - name: Upload release asset
        uses: softprops/action-gh-release@v1
        with:
          files: dist/CAN\ Library\ Generator.exe

  build-macos:
    runs-on: macos-latest
    needs: set_version
    env:
      APP_VERSION: ${{ needs.set_version.outputs.version }}
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Inject version into main.py
        run: |
          sed -i '' "s/^__version__ = .*/__version__ = \"${APP_VERSION}\"/" main.py

      - name: Build macOS app
        run: |
          python setup.py py2app

      - name: Create DMG
        run: |
          hdiutil create -volname "CAN_Library_Generator" \
                         -srcfolder "dist/CAN_Library_Generator.app" \
                         -ov -format UDZO dist/CAN_Library_Generator.dmg

      - name: Upload release assets
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/CAN_Library_Generator.app
            dist/CAN_Library_Generator.dmg

  build-linux:
    runs-on: ubuntu-latest
    needs: set_version
    env:
      APP_VERSION: ${{ needs.set_version.outputs.version }}
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgdk-pixbuf2.0-0 libglib2.0-0
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Inject version into main.py
        run: |
          sed -i "s/^__version__ = .*/__version__ = \"${APP_VERSION}\"/" main.py

      - name: Build Linux executable
        run: |
          pyinstaller --clean --onefile --windowed --add-data "png:png" --name "Can_Library_Generator" main.py

      - name: Upload release asset
        uses: softprops/action-gh-release@v1
        with:
          files: dist/can_library_generator