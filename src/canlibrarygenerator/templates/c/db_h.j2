/******************************************************************************
*
* @file         {{ ir.library_name }}_db.h
* @brief        Definitions of specific structures for CAN messages and signals
* @author       Generated using CAN Library Generator tool
* @date         {{ ir.current_date }}
*
* @copyright    (c) {{ ir.current_year }} Mobility Lab, VŠB - Technical University of Ostrava
* All rights reserved.
*
* @note         Generator version: {{ ir.generator_version }}
*
{%- if ir.dbc_versions %}
* @note         DBC Files versions:
{%- for name, version in ir.dbc_versions %}
*               - {{ name }}: {{ version }}
{%- endfor %}
*
{%- endif %}
* @details      This file was automatically generated by CAN Library Generator tool.
* Contains definitions and/or implementations of structures for working with CAN messages.
* Any manual modifications in this file will be overwritten
* during subsequent generation.
*
******************************************************************************/

#ifndef {{ ir.library_name.upper() }}_DB_H
#define {{ ir.library_name.upper() }}_DB_H

#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stddef.h>
#include <math.h>

#include "can_db_def.h"

{% for msg in ir.messages %}
#define {{ ir.library_name.upper() }}_{{ msg.name.upper() }}_ID {{ "0x%X"|format(msg.frame_id) }}
{%- endfor %}

{%- for msg in ir.messages %}

// Structure for CAN message {{ msg.name }}.
typedef struct {
    can_db_msg_t base;
{%- for sig in msg.signals %}
    can_db_sig_t *{{ sig.name }};
{%- endfor %}
} can_db_{{ msg.name.replace(' ', '') }}_t;
{%- endfor %}

// Declaration of global CAN message instances.
{%- for msg in ir.messages %}
extern can_db_{{ msg.name.replace(' ', '') }}_t {{ ir.library_name }}_{{ msg.name }};
{%- endfor %}

// Universal structure for incoming messages.
extern can_db_msg_t can_db_rx_msg;

// Global registry of all defined CAN messages.
extern can_db_msg_t* const {{ ir.library_name }}_all_messages[];

// Number of messages in the {{ ir.library_name }}_all_messages registry.
extern const size_t {{ ir.library_name }}_all_messages_count;

#endif // {{ ir.library_name.upper() }}_DB_H